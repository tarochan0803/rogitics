# road_seg コア（幾何エンジン + タイル取得 + エンドポイント）
numpy>=1.24
Pillow>=10.0
requests>=2.28
fastapi>=0.110
uvicorn>=0.29

# --- 手順1/2でモデルを差すとき（任意・別途インストール） ---
# 商用クリーンな選択肢:
#   torch>=2.1                      # 推論基盤
#   torchvision>=0.16               # torch/smp encoder 依存
#   segmentation-models-pytorch     # U-Net / DeepLabV3+ など (MIT)
# 公開学習済み道路抽出（手順1のゼロ学習評価）は各リポジトリの重み・推論コードを利用。
# --- YOLO要素検出の学習（任意） ---
#   ultralytics                  # メニュー9) YOLO学習で使用。AGPL/商用ライセンス要確認。
